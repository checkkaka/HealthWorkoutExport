name: health_workout_export
description: HealthWorkoutExport 的跨平台 Flutter 客户端。
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  archive: ^4.0.9
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0
  rust_lib_health_workout_export:
    path: rust_builder
  share_plus: ^13.3.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
